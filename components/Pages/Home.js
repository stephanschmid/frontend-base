var React = require('react');
var PropTypes = React.PropTypes;

var Home = React.createClass({

    render: function() {
        return (
            <div>Home</div>
        );
    }

});

module.exports = Home;
